# Copy to .env and fill in. setup.sh will auto-populate the GCP fields.

# GCP project (set by setup.sh)
GOOGLE_CLOUD_PROJECT=

# Vertex AI location. "global" routes to the nearest region automatically.
# Cloud Run (Step 06) requires a specific region — see tutorial.md.
GOOGLE_CLOUD_LOCATION=global

# Use Vertex AI as the backend (vs. direct Gemini API)
GOOGLE_GENAI_USE_VERTEXAI=TRUE

# Agent Engine デプロイ時に Cloud Trace へテレメトリを送る (Step 06)
GOOGLE_CLOUD_AGENT_ENGINE_ENABLE_TELEMETRY=true

# OpenTelemetry GenAI トレースに「プロンプト本文 / モデル応答本文」も含める。
# デフォルトは false でタイミングだけ。workshop ではトレースで会話内容を見たいので true。
# 本番でユーザー入力に PII が含まれる場合は false が安全。
OTEL_INSTRUMENTATION_GENAI_CAPTURE_MESSAGE_CONTENT=true

# Google Maps Platform API key (Maps Grounding Lite 有効化済み)
# ワークショップ当日に共有される (X-Goog-Api-Key ヘッダで mapstools.googleapis.com/mcp に送信)
# Section 2-2b (MCP) と 2-1 (multi-agent) の RecommendAgent で使用
MAPS_API_KEY=YOUR_MAPS_API_KEY_HERE
